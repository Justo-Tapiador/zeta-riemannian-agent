# Dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Lockfiles — keep only bun.lock (project runtime is bun)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Testing
/coverage

# Build artifacts
/dist/
/build/
/.next/
/out/

# Misc OS / editor
.DS_Store
*.pem
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo
*~

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log
dev.log
dev.out.log
server.log
research/proofs/*.log

# Secrets — NEVER COMMIT
.env
.env.*
!.env.example
.z-ai-config
.claude
local-*

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Database (SQLite) — never commit binary state
db/*.db
db/*.db-journal
db/*.sqlite
db/*.sqlite-journal
prisma/db/*.db
prisma/db/*.db-journal
prisma/db/*.sqlite
prisma/db/*.sqlite-journal

# Compiled output
dist/

# Agent runtime artifacts (keep folders via .gitkeep)
research/proofs/*.tex
research/proofs/*.pdf
research/proofs/*.meta.json
research/proofs/*.verifier.json
research/theorems/*.tex
research/theorems/*.pdf
research/theorems/*.tags.json
research/riemann-attempts/*.tex
research/riemann-attempts/*.pdf
research/riemann-attempts/*.verifier.json
research/arxiv-cache/*
!research/arxiv-cache/.gitkeep
research/hypotheses/*.tex
research/hypotheses/*.meta.json

# Keep INDEX.md tracked
!research/INDEX.md

# Internal worklog (build-time log; do not publish)
worklog.md

# IDE / editors (already covered above; keep for backward compat)
